export async function onRequestPost({request,env}){let d=await request.json();await env.BAIDU_PSYCHOLOGY_DB.prepare('INSERT INTO appointments(name,phone) VALUES(?,?)').bind(d.name,d.phone).run();return new Response('预约成功')}